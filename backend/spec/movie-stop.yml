tags:
  ['Movies']
summary: "Stop load movie by torrent hash (ID)"
parameters:
  - name: torrentHash
    type: string
    in: query
    description: Torrent ID (hash string)
responses:
  200:
    schema:
      allOf:
        - $ref: '#/definitions/AnswerAPI'
