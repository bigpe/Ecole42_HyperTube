tags:
  ['Users']
summary: "Auth User"
parameters:
  - name: login
    type: string
    in: query
  - name: password
    type: string
    in: query
responses:
  200:
    schema:
      allOf:
       - $ref: '#/definitions/UserInfo'
       - $ref: '#/definitions/AnswerAPI'